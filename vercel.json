{
  "buildCommand": "cd start && npm install && npm run build",
  "outputDirectory": "start/dist",
  "rewrites": [
    {
      "source": "/start/(.*)",
      "destination": "/start/index.html"
    }
  ]
}
